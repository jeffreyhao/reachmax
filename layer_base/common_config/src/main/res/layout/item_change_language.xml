<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:paddingVertical="17dp"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingHorizontal="20dp"
    android:background="@drawable/bg_transparent_press"
    >

    <ImageView
        android:id="@+id/iv_language_icon"
        android:layout_width="22dp"
        android:layout_height="22dp"
        android:scaleType="fitXY"
        android:padding="0.5dp"
        android:background="@drawable/bg_language_select_icon"
        tools:src="@drawable/ic_language_de"
        />

    <FrameLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginHorizontal="10dp"
        >
        <TextView
            android:id="@+id/tv_language_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start"
            android:gravity="start"
            android:textSize="16dp"
            android:textColor="@color/color_select_language"
            tools:text="الحصول على الإذن للتشغيل أولا"/>
    </FrameLayout>


    <ImageView
        android:id="@+id/iv_select_language"
        android:src="@drawable/ic_select_language"
        android:layout_marginHorizontal="6.67dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>
</LinearLayout>